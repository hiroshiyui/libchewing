[package]
name = "chewing_testhelper"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
chewing = { version = "0.13.0-alpha.0", path = "../.." }

[features]
sqlite = ["chewing/sqlite"]
